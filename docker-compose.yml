version: '3.8'

services:
  spendsense:
    build: .
    container_name: spendsense_app
    ports:
      - "8501:8501"
    volumes:
      - spendsense_data:/app/data
    env_file:
      - .env
    restart: always

volumes:
  spendsense_data:
